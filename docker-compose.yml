version: "3.9"

services:
  api:
    build: .
    ports:
      - "7860:7860"
    env_file:
      - .env
    volumes:
      - ./models:/app/models   # mount models so you can retrain without rebuilding
    restart: unless-stopped